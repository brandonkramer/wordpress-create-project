/**
 * Classes used for all packages
 */
const terminal = require('./terminal');
const logger = require('./logger');
const operator = require('./operator');

module.exports = {
    terminal,
    logger,
    operator,
};